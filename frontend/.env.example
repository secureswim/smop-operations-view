# Backend API URL (used in production builds; in dev, Vite proxy handles this)
VITE_API_URL=http://localhost:3001

# Backend port (must match backend's PORT in backend/.env)
# Default: 3001
VITE_API_PORT=3001
